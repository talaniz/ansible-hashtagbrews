from .base import *

DEBUG = True

SECRET_KEY = os.environ["SECRET_KEY"]

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'hashtagbrews',
        'USER': 'hashtagbrews',
        'PASSWORD': os.environ["DB_PASSWORD"],
        'HOST': '127.0.0.1',
        'PORT': '5432',
    }
}
